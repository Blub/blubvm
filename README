For installation instructions read INSTALL.
For licensing read COPYING.

If any of those files is missing, you can retrieve them from the git repository
from http://git.rear.endoftheinternet.org/blubvm.git
